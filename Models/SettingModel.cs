using System;

namespace Models
{
    public enum JavLibraryGetCookieMode
    {
        Easy = 0,
        MockBroswer = 1
    }

    public enum SettingSite
    {
        JavLibrarySettings = 1,
        CommonSettings = 2
    }
}
